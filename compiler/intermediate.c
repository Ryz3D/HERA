#include "includes.h"

typedef struct inter_ins {
    // TODO: abstract instruction like (add [RAM] and [LITERAL])
    int i;
} inter_ins_t;

bool cmp_inter_generate(ast_element_t *ast) {
    return;
}
